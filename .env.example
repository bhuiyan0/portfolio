# Which portfolio persona to render. Resolved at build time.
# Must be NEXT_PUBLIC_ so client components (header, mobile nav) see it too.
# Values: frontend | fullstack   (unset or unknown -> frontend)
NEXT_PUBLIC_PORTFOLIO_VARIANT=frontend
